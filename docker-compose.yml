services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  api:
    build:
      context: .
      dockerfile: src/LlmProxy.Api/Dockerfile
    ports:
      - "8080:8080"
    depends_on:
      - redis
    environment:
      - ConnectionStrings__Redis=redis:6379
